name: data-driven-reproducible-study
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - jupyter
      - pandas
      - numpy
      - matplotlib
      - seaborn
      - scikit-learn
      - statsmodels
      - pyjanitor
